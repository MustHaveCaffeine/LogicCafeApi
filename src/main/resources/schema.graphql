type Query {
  user(username: String): User
  problem(slug: String): Problem
  problems: [Problem!]
}

type User {
  firstName: String
  lastName: String
}

type Problem {
  slug: String
  title: String
  level: String
  tags: [String!]
}